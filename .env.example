# Copy to .env and fill in. .env is gitignored and never committed.
# Only one is needed; the first one found wins.
# The default endpoint in prompt_enhancer.py is Groq, so use GROQ_API_KEY
# unless you also change DEFAULT_ENDPOINT to Cerebras.
GROQ_API_KEY=
# CEREBRAS_API_KEY=
